extends ../layouts/other-pages/layout.pug

append variables
  -
    mainModifier = 'txt'
    breadCrumbsList = false

block title
  | МЕХ ЗАВОД SOTRANS

block content
  section.txt
    .container.container_sm
      h1 Политика в отношении обработки персональных данных
      .txt__content
        each el in policyList
          h2 #{el.title}
          ul
            each item in el.txt
              li #{item}

