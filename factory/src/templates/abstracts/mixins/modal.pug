mixin modal(params)
  .modal(class=params.modifier data-modal=params.dataset)
    .modal__body
      .modal__close
        img(src=require('@/assets/img/global/modal-close.svg') alt="")
      h2.modal__title #{params.title}
      block
