mixin category-grid(params)
  h3.category-grid__title #{params.title}
  .category-grid__grid
    each el in params.list
      a.category-grid__item(href=el.url)
        .category-grid__item-image
          | !{el.image}
        .category-grid__item-txt #{el.txt}
